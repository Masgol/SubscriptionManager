<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SubscriptionManager" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:fontFamily">@font/timeburner</item>

        <item name="colorPrimary">@color/white</item>
        <!-- Customize the primary variant color (optional) -->
        <item name="colorPrimaryVariant">#1E1F22</item>
        <!-- Customize the secondary color -->
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/black</item>


    </style>

    <style name="Theme.SubscriptionManager" parent="Base.Theme.SubscriptionManager" />
</resources>